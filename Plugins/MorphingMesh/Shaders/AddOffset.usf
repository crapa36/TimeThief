#include "/Engine/Public/Platform.ush"

RWStructuredBuffer<uint> OffsetBuffer;
uint OffsetBufferSize;

[numthreads(1, 1, 1)]
void MainCS()
{
	uint PrefixSum = 0;
	for (int i = 0; i < OffsetBufferSize; ++i)
	{
		uint temp = OffsetBuffer[i];
		OffsetBuffer[i] = PrefixSum;
		PrefixSum += temp;
	}
}
